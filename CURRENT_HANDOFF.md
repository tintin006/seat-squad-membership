# SEAT Squad Membership — Current Handoff

Last updated: 2026-04-23 09:20 PM EDT
Status: Foundation scaffold complete. New Next.js App Router repo exists locally with Remix Academics warm editorial theme tokens and starter shell. Not yet connected to Supabase, Stripe, or Vercel.

---

## Repo

Path: `/Users/chlinder/seat-squad-membership`

Stack scaffolded:

- Next.js App Router
- TypeScript
- Tailwind CSS v4
- ESLint
- npm
- `src/` directory
- Import alias: `@/*`

Generated by:

```bash
npx create-next-app@latest seat-squad-membership --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --yes
```

---

## Completed Today

### 1. Next.js scaffold

Created a fresh app/repo at:

`/Users/chlinder/seat-squad-membership`

The app currently runs as a standalone local Next.js project. The repo has its own git history initialized by create-next-app.

### 2. Remix Academics warm editorial design tokens

Updated:

- `src/app/globals.css`
- `src/app/layout.tsx`
- `src/app/page.tsx`
- `public/brand/asterisk.png`

Design tokens now match the Remix Academics warm editorial system:

- Primary: Princeton Orange `#fb8b24`
- Background: warm tan cream `#E8D0B4`
- Foreground: warm brown `#3D2E22`
- Card: `#DEC4A8`
- Surface Low: `#D9BF9E`
- Surface High: `#C9AB88`
- Secondary: Crimson Violet `#5f0f40`
- Accent: Dark Teal `#0f4c5c`
- Dark mode class: `.remix-dark` with warm ink `#1A1410`
- Radius: `0.5rem`
- Body font: Inter via `next/font/google`
- Display stack: `General Sans`, Inter, system-ui fallback
- Brand symbol: orange asterisk PNG copied from Remix Academics site

Important: no old `#FF6C01` orange, no default pure-black dark theme, no sharp-corner default.

### 3. Starter shell

Replaced the default create-next-app screen with a SEAT Squad scaffold preview:

- Header with SEAT Squad / Remix Academics identity
- Preview nav: Home, Classroom, The Crate, Events, Members, Tutors
- Hero describing the new membership app foundation
- Skool-inspired layout preview:
  - left nav
  - centered community feed
  - right rail
- First three build layers:
  - Auth + onboarding
  - Community shell
  - Resources + learning

### 4. Roadmap copied into repo

Copied the build roadmap to:

`BUILD_ROADMAP.md`

Original source remains:

`/Users/chlinder/seat-squad-membership-build-roadmap.md`

---

## Current Files to Know

- `src/app/globals.css` — theme contract and warm editorial CSS tokens
- `src/app/layout.tsx` — metadata, viewport, Inter font, app root layout
- `src/app/page.tsx` — temporary scaffold homepage / app-shell preview
- `public/brand/asterisk.png` — canonical SEAT/Remix asterisk symbol
- `BUILD_ROADMAP.md` — roadmap for full membership build
- `public/BUILD_ROADMAP.md` — public copy linked from the starter shell
- `CURRENT_HANDOFF.md` — this file

---

## Next Recommended Steps

1. Run verification:
   - `npm run lint`
   - `npm run build`
2. Commit the scaffold.
3. Add Supabase packages:
   - `@supabase/ssr`
   - `@supabase/supabase-js`
4. Create env template:
   - `.env.example`
5. Start database work:
   - `supabase/migrations/001_seat_enums_and_helpers.sql`
   - `supabase/migrations/002_seat_profiles_and_subscriptions.sql`
6. Build auth/onboarding before feed/resource/tutor work.

---

## Architecture Decision Locked

SEAT Squad should share the existing Remix Academics / Mixtape360 Supabase project, but all SEAT product data should use `seat_*` table names.

Do not overload existing `public.profiles.tier` for SEAT membership. Use `public.seat_profiles.tier` and helper functions for SEAT-specific role/tier checks.

---

## Verification Notes

Pending at time of writing:

- `npm run lint` passes.
- `npm run build` passes.
- Added `turbopack.root = process.cwd()` in `next.config.ts` to prevent Next from treating `/Users/chlinder` as the workspace root because of parent lockfiles.
- Local smoke test passes at `http://127.0.0.1:3000/` with status 200 and expected SEAT Squad/scaffold content present.
- Dev server command for Next 16 local bind: `npm run dev -- -H 127.0.0.1` (`--host` is not accepted).
- Initial scaffold committed locally in this repo.
